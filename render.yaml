services:
  - type: web
    name: tigergraph-fraud-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/server.py
    envVars:
      - key: PORT
        value: 10000
